export const metadata = {
    title: 'Login',
    description: 'Sign in to your MV Webservice account to manage your projects and payments.',
    robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
    return children;
}
